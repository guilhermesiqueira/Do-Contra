<div class="linha">
	<% @reviews.each do|review| %>
		<div class="col-sm-6 col-md-3">
			<div class="thumbnail">
				<%= link_to (image_tag review.image.url(:medium), class: 'image'), review %>
			</div>
		</div>
	<% end %>
</div>
<% if current_user.try(:admin?) %>


<% end %>
